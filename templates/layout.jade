!!! 5
block vars
  - var bodyclass = null;
html(lang='en')

  head
    block head
      meta(charset='utf-8')
      meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
      meta(name='viewport', content='width=device-width, initial-scale=1.0')
      meta(name='keywords', content='personality,mindfulness,quantified self,psychology')
      meta(name="description", content="Fun, scientifically validated games help you discover your personality and other traits.")
      script.
        window.pageLoadStart = (new Date()).getTime()
      title
        block title
          = locals.name
      link(rel='alternate', href=locals.url+'/feed.xml', type='application/rss+xml', title=locals.description)
      link(rel='stylesheet', href='//fonts.googleapis.com/css?family=Karla:400,700')
      link(rel='stylesheet', href='/@@buildDir/all-min.css')

  body(class=bodyclass)

    include header

    div.content
      block content
        p Default content block contents.

    block footer
      include footer



    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
              m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', '@@googleAnalyticsKey', 'tidepool.co');
      ga('send', 'pageview');


      // Track Site Performance
      trackPerf = function(){ // Wait until the performance numbers come in before we log them
          var data = {}
          if(performance && performance.timing) {
              data.latency =       performance.timing.responseEnd  - performance.timing.fetchStart;
              data.pageLoad =      performance.timing.loadEventEnd - performance.timing.responseEnd;
              data.totalLoadTime = performance.timing.loadEventEnd - performance.timing.navigationStart;
              ga('send', 'event', {
                  eventCategory: 'performance',
                  eventAction: 'log',
                  eventLabel: 'latency',
                  eventValue: data.latency
              });
              ga('send', 'event', {
                  eventCategory: 'performance',
                  eventAction: 'log',
                  eventLabel: 'pageLoad',
                  eventValue: data.pageLoad
              });
          } else {
              if (window.pageLoadStart) { data.totalLoadTime = (new Date()).getTime() - window.pageLoadStart }
          };
          ga('send', 'event', {
              eventCategory: 'performance',
              eventAction: 'log',
              eventLabel: 'totalLoadTime',
              eventValue: data.totalLoadTime
          });
      }
      window.onload=trackPerf
